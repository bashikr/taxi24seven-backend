CREATE TABLE users (
        id INT PRIMARY KEY,
        firstName VARCHAR,
        lastName VARCHAR,
        username VARCHAR,
        phone VARCHAR,
        email VARCHAR,
        password VARCHAR,
        role VARCHAR,
        joinDate DATE
      );
